version: '1.0'

# This creates database records and a running scheduler will do the deployments
network:
  name: network-1
  queues:
    pyfi.queue1:
    pyfi.queue2:
    pyfi.queue3:
  nodes:
    radiant:
      hostname: radiant
      ssh:
        key: '/home/darren/.ssh/id_rsa.pub'
        user: 'darren'
      path: /home/darren/node1
      agents:
        agent1:
          parameters:
            pool: 5
            size: 10
          processors:
            proc2: 
              branch: main
              gitrepo: 'https://radiantone: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@github.com/radiantone/pyfi-private#egg=pyfi'
              commit:
              module: pyfi.processors.sample
              beat: true
              workers: 6
              sockets:
                socket1:
                  queue: 
                    name: pyfi.queue2
                  task:
                    function: do_this
    
# This will create work records which the scheduler will deploy
work:
  


# This will create immediate executions using the API models
executions: