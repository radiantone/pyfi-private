# Run ssh commands to start agents on your hosts with pyfi installed. This is a convenience script for development only
# In production your server builds would have pyfi installed and supervisord configured to launch the agent
